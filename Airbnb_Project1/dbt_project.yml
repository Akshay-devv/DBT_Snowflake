name: 'Airbnb_Project1'
version: '1.0.0'

profile: 'Airbnb_Project1'

# Paths
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

# Directories to be removed by `dbt clean`
clean-targets:
  - "target"
  - "dbt_packages"

# Model configurations
models:
  Airbnb_Project1:
    Raw:
      +materialized: table
      +schema: raw
    Silver:
      +materialized: table
      +schema: silver
